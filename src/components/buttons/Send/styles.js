import { Flex } from "rebass";
import styled from "styled-components";

const ModalContainer = styled(Flex)`
  flex-direction: row;
`;

const ButtonContainer = styled(Flex)`
  width: 180px!important;
  padding: 0!important;
`;

export { ButtonContainer, ModalContainer };

